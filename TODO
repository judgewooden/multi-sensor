TODO
----
- Program all sensors
- Consider removing DATA/CONTROL channels make 1
- numpy ??? // panda for calculations
- figure out all types of returns from current code
- make mRouter a control program unit
- redo in finite state machine
- make filter option for all programs not only control (-l) ---- @@ is ALL
- Think how to do a proxy (one program updating two or more sensors)
- Consider that some sensors will not be stored....

- Consider loading last data-Interval from database when user opens dashboard
- Add a feature to jphconfig that validates all JSON

- Build a program to configure the JSON
 	- double Check the proxies

DONE
- All logger per host / per unit
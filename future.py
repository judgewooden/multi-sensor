#!/usr/bin/env python

import jph
import os

code="A1"

def main():
    try:
        m=jph.start(os.getenv("JPH_CONFIG", code))

        while(jph.monitor()):

    except:
        print("something wrong with your config")
        return




    f=socket.socket(a,b,c)
    socket.set(f,d)
    socket.set(f,"function()")
    socket.set(f,8)

def function()


if __name__ == '__main__':
    main()